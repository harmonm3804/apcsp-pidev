#include <stdio.h>

int main()
{
char strl1 [] = "abcdefghijklmnopqrstuvwxyz"
char str2 [27];
char str3 [54];
char c = 'a';
for (int i = 0; i < 27; i++)

{
  skr2[i]  c:
  c++;
}

{

if (strcpy(str1, str2) == 0)
   printf("the strings are identical\n");
else
   printf("the strings are different\n");
}

{
for (int i = 0; i < 27; i++)
str1[i] = 

{
   strcpy(str3, str1);
   strcat(str3, str2);

   printf("String 1 is %s\n", str1);
   printf("String 2 is %s\n", str2);
   printf("String 3 is %s\n", str3);  
